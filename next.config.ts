/** @type {import('next').NextConfig} */
const nextConfig = {
  // Only Next.js options go here, like:
  reactStrictMode: true,
  // ...any other Next.js-specific options...
}

module.exports = nextConfig
